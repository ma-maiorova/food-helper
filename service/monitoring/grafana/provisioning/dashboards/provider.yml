apiVersion: 1

providers:
  - name: 'Food Helper'
    orgId: 1
    folder: 'Food Helper'
    folderUid: 'food-helper-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
