ktor {
  deployment {
    port = 8080
  }

    application {
        modules = [ org.example.ApplicationKt.module ]
    }

  database {
    jdbcUrl = "jdbc:postgresql://127.0.0.1:5435/food_helper"
    driverClassName = "org.postgresql.Driver"
    username = "food_user"
    password = "food_pass"
    maximumPoolSize = 10
  }
}
